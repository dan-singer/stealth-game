using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Guidance : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // check for user input (key.onHold, mouse.right.onHold)

        // bring guidance into focus

        // look through array for any card pieces still remaining
        
        // find the closest card piece still that is still active

        // pointer.turnToLookAt(Vector3(self.x, direction.y, self.z)
    }
}
